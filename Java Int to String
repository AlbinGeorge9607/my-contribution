# my-contribution


//Write your code here
String s = "";
   int digit, tmp;

   tmp = n;
   if(tmp<0){
       tmp = -tmp;
   }

   while(tmp >= 10){
       digit = tmp % 10;
       tmp = tmp / 10;
       s = digit + s;
   }
   s = tmp + s;

   if(n < 0){
       s = "-" + s;
   }
